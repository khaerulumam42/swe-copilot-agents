[project]
name = "swe-copilot-agents"
version = "0.1.1"
description = "GitHub Copilot custom agents for orchestrating the complete software development lifecycle"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [
    { name = "Khaerul Umam", email = "khaerul@example.com" }
]
keywords = ["github", "copilot", "agents", "ai", "development"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Software Development",
]

dependencies = []

[project.scripts]
awesome-copilot = "awesome_copilot.cli:main"

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "pytest-cov>=4.1.0",
    "black>=24.0.0",
    "ruff>=0.1.0",
]

[project.urls]
Homepage = "https://github.com/khaerulumam42/agent-generator"
Repository = "https://github.com/khaerulumam42/agent-generator"
Issues = "https://github.com/khaerulumam42/agent-generator/issues"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build]
include = [
    "awesome_copilot/**/*.py",
    "awesome_copilot/**/*.pth",
    "agents/*.agent.md",
    "copilot-agent-sources/*",
    "skills/pdf-slides/**/*.py",
    "README.md",
    "CLAUDE.md",
]

[tool.hatch.build.targets.wheel]
packages = ["awesome_copilot"]

[tool.hatch.build.targets.wheel.shared-data]
"agents" = "share/swe-copilot/agents"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.black]
line-length = 100
target-version = ['py311']
